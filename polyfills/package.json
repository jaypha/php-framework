{
  "name": "polyfills",
  "version": "0.1.0",
  "description": "Compiles polyfills",
  "main": "",
  "scripts": {
    "sass-dev": "node-sass --include-path node_modules --include-path src",
    "sass-dist": "node-sass --output-style compressed --include-path node_modules --include-path src",
    "rollup-dev": "rollup -c",
    "rollup-dist": "rollup -c --compact",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jason den Dulk",
  "devDependencies": {
    "es6-promise": "^4.2.6",
    "node-sass": "^4.11.0",
    "rollup": "^1.7.4",
    "rollup-plugin-commonjs": "^9.2.2",
    "rollup-plugin-node-resolve": "^4.0.1",
    "rollup-plugin-uglify": "^6.0.2",
    "whatwg-fetch": "^3.0.0"
  }
}
